# Tidy up and publish remote-control
# Change chain-of-command  `then` to `always`
# Automate documentation generation, publishing etc
# Teamcity integration for github pull requests
# Publish all the modules on npm
